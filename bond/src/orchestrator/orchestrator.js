const validateBuildId = function validateBuildId(buildId) {
    return buildId == 'abc';
};

const getBuildId = function getBuildId() {
    return 'abc';
}

module.exports = {
    validateBuildId,
    getBuildId
};
